<style>
    .glossary-term {
        position: relative;
        cursor: help;
        font-weight: 500;
        color: #2563eb;
        border-bottom: 1px dotted currentColor;
        text-decoration: none;
        &::after {
            content: "ⓘ";
            font-size: 0.7em;
            margin-left: 0.15rem;
            vertical-align: super;
            opacity: 0.7;
        }
        &:hover {
            color: #1d4ed8;
            border-bottom-style: solid;
        }
        &:focus-visible {
            outline: 2px solid currentColor;
            outline-offset: 2px;
        }
    }
</style>
<script>
  window.chamiloGlossaryConfig = {
    courseId: {{ course ? course.id : (app.request.get('cid') ?? 'null') }},
    sessionId: {{ session ? session.id : (app.request.get('sid') ?? 'null') }},
    resourceNodeParentId: {{ resourceNodeParentId ?? 'null' }},
    termsEndpoint: "/api/glossaries"
  };
</script>
<script src="/build/runtime.js"></script>
<script src="/build/glossary_auto.js"></script>
