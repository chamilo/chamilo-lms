imports:
    - { resource: default.yml }
    
doctrine:
    dbal:
        driver:   pdo_sqlite
        memory:   true

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        entity_managers:
            default:
                auto_mapping: true
