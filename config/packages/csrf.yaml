# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            - logout
            # Single token id used by CsrfProtectionListener to guard every
            # state-changing API Platform operation and legacy AJAX endpoint.
            # Being stateless, it validates through the Origin/Sec-Fetch-Site
            # headers alone, so it never touches the session and no caller has
            # to send anything.
            - chamilo_request
